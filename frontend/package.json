{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev -w app",
    "build": "npm run build -w app"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/app"
  ]
}
